# Environment files — never commit private keys or secrets
.env
.env.*
!.env.example
!.env.*.example
.env.local

# Hardhat compilation and caching output
artifacts/
cache/
typechain-types/

# Private key and mnemonic files
*.key
*.pem
mnemonic.txt
secret.txt

# Coverage reports
coverage/
coverage.json

# Node dependencies
node_modules/

# Debug logs
npm-debug.log*
yarn-debug.log*
